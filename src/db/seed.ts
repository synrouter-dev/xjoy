/**
 * Database seeder.
 * Usage: npm run db:seed
 *
 * Placeholder — will populate the DB with KJV text after the data pipeline is built.
 */
async function main() {
  console.log("Seed: no data to seed yet. Implement after data pipeline is ready.");
}

main();
