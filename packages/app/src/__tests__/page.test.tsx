/**
 * Basic smoke test — verifies the test harness works.
 */
import { describe, it, expect } from "@jest/globals";

describe("smoke test", () => {
  it("passes", () => {
    expect(true).toBe(true);
  });
});
